general {
        colors = true
        interval = 1
}

order += "battery all"
order += "tztime local"

battery all {
        format = "%percentage"
}

tztime local {
        # Formats as: YYYY-MM-DD HH:MM:SS
        format = "%Y-%m-%d %H:%M:%S"
}
